export enum OrderType {
    YES = 'true',
    NO = 'false'
}

export enum OrderDirection {
    None = 'none',
    ASC = 'ASC',
    DESC = 'DESC'
}

export enum QueryConditions {
    OR = 'or',
    AND = 'and',
    LIKE = 'like',
    SELECT = 'select'
}
