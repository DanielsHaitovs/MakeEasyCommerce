export enum ProductTypes {
    ALL = 'all',
    SIMPLE = 'simple',
    CONFIGURABLE = 'configurable',
    PERSONALIZED = 'personalized',
    GROUPED = 'grouped',
    VARIATION = 'variation',
    VIRTUAL = 'virtual',
}

export enum GroupTypes {
    BUNDLE = 'bundle',
    RELATED = 'related',
    CROSS_RELATED = 'cross-related',
    CONTENT_BLOCK = 'content-block',
    SLIDER = 'slider',
    CAMPAIGN = 'campaign',
    SALES = 'sales',
}

export enum ProductVisibility {
    AVAILABLE = 'available',
    CATALOG = 'catalog',
    SEARCH = 'search',
    NOT_VISIBLE = 'not_visible',
    SOLD_OUT = 'sold_out',
}
