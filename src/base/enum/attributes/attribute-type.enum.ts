export enum AttributeType {
    String = 'String',
    Boolean = 'Boolean',
    Number = 'Number',
    JSON = 'JSON',
    Date = 'Date',
}

export enum AttributeRuleType {
    Front = 'front',
    Back = 'back',
}

export enum JoinAttributeRelations {
    All = 'all',
    Options = 'options',
    rule = 'rule',
}
